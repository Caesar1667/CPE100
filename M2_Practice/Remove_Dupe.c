#include <stdio.h>

int ReDupe(int arr[], int size){
    int arr1[size], count = 0;
    if(n == 1 || arr[0] != arr[1]) arr[count++]
}

int main(){
    int n;
    scanf("%d", &n);
    int x[n];
    for(int i = 0; i < n; i++){
        scanf("%d", &x[i]);
    }
    for(int i = 0; i < n; i++){
        printf("%d\n", x[i]);
    }
    return 0;
}