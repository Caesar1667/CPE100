#include <stdio.h>
int Circle(){
    
}
int main(){
        int n, k, i;
        scanf("%d%d", &n, &k);
        int m[n];
        for(i = 0; i < n; i++){
            m[i] = i+1;
        }
    return 0;
}